cron:
- description: weekly etl job
  url: /etl/weekly
  target: etl
  schedule: every sunday 04:30
  timezone: America/Los_Angeles
  retry_parameters:
    min_backoff_seconds: 60
    max_doublings: 5